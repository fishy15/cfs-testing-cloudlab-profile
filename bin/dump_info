#!/bin/bash

cd "$(dirname "$0")/.."

TOPOLOGY="$TOPOLOGY" runvm lfs -s -S  >/dev/null 2>&1 &
gdb kbuild/vmlinux --batch -ex "py TOPOLOGY=\"$TOPOLOGY\"" -x kernel/dumper.py
killall qemu-system-x86_64
